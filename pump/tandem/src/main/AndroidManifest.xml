<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.nightscout.androidaps.plugins.pump.tandem">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application>
        <activity
            android:name="info.nightscout.androidaps.plugins.pump.tandem.info.nightscout.aaps.pump.tandem.ui.TandemPumpBLEConfigActivity"
            android:theme="@style/AppTheme"
            android:exported="false">
            <intent-filter>
                <action android:name="info.nightscout.androidaps.plugins.pump.tandem.dialogs.TandemPumpBLEConfigActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--        <uses-library android:name="org.apache.http.legacy" android:required="false"/>-->
    </application>

</manifest>
